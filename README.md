# docker-sandbox
docker-sandbox GPU-enabled sandbox environments for safe code execution.
# Docker Sandbox

The **Gitdigital Products** container playground.  
Use this repo to run isolated environments for testing services, models, and APIs.

## 🚀 Quickstart
```bash
# Build container
docker-compose build

# Run container
docker-compose up -d

# Get shell inside
docker exec -it docker_sandbox bash
