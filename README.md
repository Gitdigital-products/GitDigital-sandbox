# GitDigital Sandbox
Secure Docker-based runtime for executing user models.  
Supports CPU + GPU runs with resource isolation.

## Features
- Per-execution containers
- Filesystem isolation
- GPU pass-through
